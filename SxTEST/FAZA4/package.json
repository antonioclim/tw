{
  "name": "sxtest-faza4",
  "version": "0.4.0",
  "description": "FAZA4 — API public minimal & validări (Zod) + teste contract (Vitest + Supertest)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "supertest": "^7.1.1",
    "vitest": "^3.2.4"
  }
}
